"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _serverlessExpress = _interopRequireDefault(require("@vendia/serverless-express"));

var _app = _interopRequireDefault(require("./app"));

var serverlessApp = (0, _serverlessExpress["default"])({
 app: _app["default"]
});

var handler = function handler(event, context) {
 // eslint-disable-next-line no-console
 console.log('EVENT', JSON.stringify(event)); // eslint-disable-next-line no-console

 return serverlessApp(event, context);
};

exports.handler = handler;