export = toCamelCase;
/**
 *Utility function to convert project name into camelCase
 *
 * @param {string} inputString
 * @return {string}
 */
declare function toCamelCase(inputString: string): string;
